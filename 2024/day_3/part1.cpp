#include <algorithm>
#include <bits/stdc++.h>
#include <vector>

using namespace std;

int main(int argc, char *argv[]) {
  string data;
  int ans = 0;
  ifstream myFile;
  myFile.open("data.txt");
  bool sorted = false;

  while (getline(myFile, data)) {
  }
  myFile.close();
  cout << ans;
  return 0;
}
